# Bascule vers le backend local (Node/Express/MongoDB, dossier ./backend)
# Copier ce fichier en .env.local puis relancer `npm run dev`.
# Tant que cette variable est absente, l'app continue d'utiliser Lovable Cloud.
VITE_API_URL=http://localhost:4000
